import PagePlaceholder from '../components/PagePlaceholder.tsx'

export default function Watchlists() {
  return (
    <PagePlaceholder title="Watchlists" phase="Phase 2">
      Multiple watchlists, each with independent settings — refresh interval,
      notification channels, quiet hours and default alert thresholds — via the
      one-row-per-watchlist <code>watchlist_settings</code> table.
    </PagePlaceholder>
  )
}
